# Archive Guide

`docs/plans/archive/` 只存放已经被吸收、替代或过期的专题设计与执行草稿。

规则：

- 这里的 dated docs 不是当前生效方案。
- 当前 truth 只看 `docs/plans/` 顶层的 canonical `st-xx-*.md`。
- 如果 archive 里的某份材料仍有价值，应把仍然有效的结论并回对应的 canonical plan，而不是把 archive 重新当成活文档引用。
